📘 README con correcciones
1. Nombre de paquete y clase
Cambiar package org.exmple; por package org.example;

Renombrar clase Mian a Main

2. Variables con nombres correctos
nombreProgramdor → nombreProgramador

corre → correo

pas → pais

tipoContrto → tipoContrato

nivelExperencia → nivelExperiencia

edd → edad

aniosExperncia → aniosExperiencia

tarifaHoraFial → tarifaHoraFinal

clente1 → cliente1

bonuCliente1/2/3 → bonusCliente1/2/3

descuento y impuesto → descuentos, impuestos

totalFinl → totalFinal

fechaActul → fechaActual

3. Inicialización de horas de proyectos
Asignar valores a horasProyecto1, horasProyecto2, horasProyecto3 desde scanner.nextInt() antes de calcular pagos.

4. Lectura de datos del scanner
Después de scanner.next(), usar scanner.nextLine() para limpiar el buffer cuando se necesite.

Combinar next() y nextLine() apropiadamente para no omitir entradas.

5. Consistencia y cálculo
Usar las variables bien nombradas para calcular tarifaHoraFinal.

Calcular pagoProyectoX = (horasProyectoX * tarifaHoraFinal) + bonusClienteX.

6. Formato del reporte
Ajustar impresión: nombres correctos, concatenación adecuada y variables corrigidas.
